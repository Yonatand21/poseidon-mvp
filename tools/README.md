# tools (top-level)

Repo-wide developer tools. Not shipped with the runtime.

Distinct from `poseidon-sim/tools/` which is application-scoped (CAD
pipeline, archetype preview).

## Contents

| File | Purpose |
| --- | --- |
| `check_layer_permissions.py` | CI lint placeholder enforcing AGENTS.md Rule 1.1. See the script docstring for the planned implementation. |
